// 报文转 Text - Background Script (minimal)
// No active logic needed; all functionality is in popup.js

chrome.runtime.onInstalled.addListener(() => {
  console.log("[报文转 Text] Extension installed");
});
